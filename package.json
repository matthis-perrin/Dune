{
  "name": "dune",
  "private": true,
  "license": "UNLICENSED",
  "devDependencies": {
    "prettier": "^2.5.1",
    "source-map-support": "^0.5.16",
    "tslint": "^6.1.1",
    "tslint-react": "^4.2.0",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "cm": "cd client-main && npm run build:dev",
    "cr": "cd client-renderer && npm run build:dev",
    "sm": "cd server-main && npm run build:dev",
    "sr": "cd server-renderer && npm run build:dev",
    "start": "cd client-main && yarn start",
    "version": "node ./version.js",
    "build": "rm -rf ./client-main/out && cd client-main && yarn electron-builder && open .",
    "install-all": "npm install && cd webpack && npm install && cd ../client-main && npm install && cd ../client-renderer && npm install && cd ../server-main && npm install && cd ../server-renderer && npm install && cd .. && cd shared && npm install && cd ..",
    "pretty": "prettier -c .prettierrc --write \"./**/*.{ts,tsx}\""
  }
}
