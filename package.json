{
  "name": "dune",
  "devDependencies": {
    "prettier": "^2.0.2",
    "source-map-support": "^0.5.16",
    "tslint": "^6.1.1",
    "tslint-react": "^4.2.0",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "install-all": "npm install && cd webpack && npm install && cd ../client-main && npm install && cd ../client-renderer && npm install && cd ../server-main && npm install && cd ../server-renderer && npm install && cd .. && && cd shared && npm install && cd ..",
    "pretty": "prettier --write \"./**/*.{ts,tsx}\""
  }
}
